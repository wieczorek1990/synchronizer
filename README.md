# synchronizer
Synchronizer for stations.

## Debug mode
To run in debug mode pass or set environment variable `DEBUG` 1 value.

```shell
cd server
DEBUG=1 ./bin/run.sh
```

```shell
cd client
DEBUG=1 ./bin/run.sh
```
